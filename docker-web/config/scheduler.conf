# NetDiag scheduler configuration.
# The scheduler logs in as the diagnostics operator over SSH to run the
# nightly reachability sweep. Credentials live here so the cron wrapper can
# read them non-interactively.

[scheduler]
enabled = true
interval_minutes = 60

[operator]
host = 127.0.0.1
port = 22
username = diag
password = msCUXbubEdnMpXFL74CyD3uK
